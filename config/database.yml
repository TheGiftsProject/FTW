defaults: &defaults
  adapter: mysql2
  username: root
  password:
  encoding: utf8

production:
  database: ftw
  <<: *defaults

development:
  database: ftw
  <<: *defaults

test:
  database: ftw
  <<: *defaults