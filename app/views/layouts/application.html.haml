!!!
%html
  %head
    %title FTW
    != stylesheet_link_tag :application
    != javascript_include_tag :application
    != stylesheet_link_tag "http://fonts.googleapis.com/css?family=Press+Start+2P"

    != csrf_meta_tag
  %body
    .navbar.navbar-fixed-top
      .navbar-inner
        .container
          %a.btn.btn-navbar{data: {toggle: "collapse", target: ".nav-collapse"}}
            .icon-bar
            .icon-bar
            .icon-bar
          %a.brand FTW
          %span.pull-left.description An RPG/Project Management hybrid.<br> Quest through tasks, kill the bugs, get loot - woot.
          - if @current_character.present?

            %ul.nav.pull-right
              %li.dropdown#account-menu
                %a.dropdown-toggle{data: {toggle: "dropdown", href: "#account-menu"}}
                  .nav-text Logged in as
                  %b.caret.pull-right
                  .account-name= @current_character.name
                %ul.dropdown-menu
                  %li
                    %a= link_to "Dashboard", dashboard_path
                  %li.divider
                  %li
                    %a= link_to "Logout", logout_path
    .container
      - flash.each do |key,val|
        .flash{:class => key}
          != val
      != yield
